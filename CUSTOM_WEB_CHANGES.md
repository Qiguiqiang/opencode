# OpenCode Web 自定义版本说明

本仓库的 `custom-web-1.17.18` 分支基于官方 OpenCode `v1.17.18`，包含针对项目较多、会话较多以及手机单手操作场景的 Web 前端调整。

这不是 OpenCode 官方发布版本，也不能通过官方 npm 包或官方 EXE 自动获得这些界面修改。需要使用本分支自行构建 `packages/app`。

## 修复和改进

### 首次加载和按需读取

- 首页优先加载项目和会话摘要列表，不再因为展示列表而预取完整会话消息和 Markdown。
- 用户打开具体会话后，才读取该会话的消息内容。
- 首页和悬浮项目菜单使用精确的会话摘要数量上限，避免额外超量读取。
- 项目目录的 path、LSP、provider 和 reference 查询延迟到该目录真正启动时执行。
- 悬浮菜单中的异常长会话标题超过 240 个字符时不直接渲染，使用默认会话名称代替。

### 项目和会话切换

- 项目列表显示项目名称和压缩后的目录路径，名称前缀相同时仍可根据路径区分。
- 当前项目具有更明显的选中状态；会话区域显示当前项目名称、路径和加载状态。
- 桌面端保留原有紧凑项目栏；展开后显示完整项目列表。手机端始终使用适合触控的名称和路径列表。
- 桌面端和手机端分别记录项目使用次数与最近使用时间，常用项目自动排在前面。
- 保留原有项目切换入口，并增加可拖动的悬浮项目按钮。
- 悬浮按钮先选择项目，再显示该项目的会话摘要；支持返回项目列表，并在选择会话后打开对应内容。
- 悬浮按钮可以停留在屏幕中间，只有靠近左右边缘时才吸附；桌面端和手机端分别保存位置。
- 拖动和点击使用移动阈值区分，避免移动按钮时误打开菜单。
- 文本输入框获得焦点时，悬浮按钮变为圆形半透明状态；操作悬浮按钮会先取消输入焦点，避免手机输入法意外弹出。
- 当前实现使用普通列表菜单，不包含转轮或径向菜单。

### 最新 UI 自动授权

- 修复最新 UI 首页没有活动 `sessionID` 时，“自动接受权限”开关被错误禁用的问题。
- 有活动会话时继续使用会话目录和会话级授权。
- 首页没有活动会话时，使用当前选中项目目录和目录级授权。
- 会话存在但目录尚未解析时保持禁用，避免把授权错误应用到其他项目。

## 兼容性和数据

- 修改范围主要位于 `packages/app`，继续使用官方 OpenCode CLI、server API 和数据格式。
- 不修改 `opencode.db` 数据库结构，不执行数据库迁移，也不会复制或替换用户历史记录。
- 不修改或替换官方 OpenCode EXE。
- 同一个 OpenCode 数据目录可以继续由官方桌面端、CLI 和本 Web 前端读取。

建议在首次部署前备份 OpenCode 数据目录。虽然本分支没有数据库迁移，但任何非官方构建都应保留可恢复副本。

## 获取和构建

```bash
git clone https://github.com/Qiguiqiang/opencode.git
cd opencode
git checkout custom-web-1.17.18
bun install

cd packages/app
bun run typecheck
bun run build
```

正式前端输出位于 `packages/app/dist`。该目录需要连接到兼容的 OpenCode server；本分支不会生成或替换官方桌面 EXE。

本地开发时分别启动后端和前端：

```bash
# 终端 1
cd packages/opencode
bun run --conditions=browser ./src/index.ts serve --port 4096

# 终端 2
cd packages/app
bun dev -- --port 4444
```

打开 `http://localhost:4444`。前端默认连接 `http://localhost:4096`。

## 验证结果

- 自动授权目标测试：13 项通过。
- 相关应用测试：32 项通过。
- `packages/app` 类型检查通过。
- `packages/app` 正式构建通过。
- 实际浏览器验证中，自动授权开关从可用的关闭状态成功切换为开启状态，控制台没有相关 warning 或 error。

## 与官方版本对比

- 官方基线：`v1.17.18`
- 自定义分支：`custom-web-1.17.18`
- GitHub 对比：[v1.17.18...custom-web-1.17.18](https://github.com/Qiguiqiang/opencode/compare/v1.17.18...custom-web-1.17.18)

后续同步官方版本时，应先重新运行应用测试、类型检查和浏览器交互验证，再更新自定义分支。
