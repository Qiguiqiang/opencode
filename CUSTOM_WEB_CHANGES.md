# OpenCode Web 自定义版本说明

本仓库的 `custom-web-1.17.20` 分支基于官方 OpenCode `v1.17.20`，包含针对项目较多、会话较多以及手机单手操作场景的 Web 前端调整。

这不是 OpenCode 官方发布版本，也不能通过官方 npm 包或官方 EXE 自动获得这些界面修改。需要使用本分支自行构建 `packages/app`。

## 修复和改进

### 官方 v1.17.20 合并结果

- 已合入官方按提示选择模型、新版附件和评论卡片、Review 面板及文件树改进。
- 已合入首页会话中键打开新标签页和新版项目编辑弹窗。
- 已合入 OpenAI pro 推理、GPT-5.6 OAuth 上下文限制、Luna Responses Lite OAuth 和 Azure AI GPT-5.6 支持。
- 官方版本没有覆盖目录级自动授权、Windows 授权键兼容、项目名称与路径列表、常用排序、悬浮切换和会话内容按需加载，因此这些本地实现继续保留。

### 首次加载和按需读取

- 首页优先加载项目和会话摘要列表，不再因为展示列表而预取完整会话消息和 Markdown。
- 用户打开具体会话后，才读取该会话的消息内容。
- 首页和悬浮项目菜单使用精确的会话摘要数量上限，避免额外超量读取。
- 项目目录的 path、LSP、provider 和 reference 查询延迟到该目录真正启动时执行。
- 悬浮菜单中的异常长会话标题超过 240 个字符时不直接渲染，使用默认会话名称代替。

### 项目和会话切换

- 项目列表显示项目名称和压缩后的目录路径，名称前缀相同时仍可根据路径区分。
- 当前项目具有更明显的选中状态；会话区域显示当前项目名称、路径和加载状态。
- 桌面端保留原有紧凑项目栏；展开后显示完整项目列表。手机端始终使用适合触控的名称和路径列表。
- 桌面端和手机端分别记录项目使用次数与最近使用时间，常用项目自动排在前面。
- 保留原有项目切换入口，并增加可拖动的悬浮项目按钮。
- 悬浮按钮先选择项目，再显示该项目的会话摘要；支持返回项目列表，并在选择会话后打开对应内容。
- 悬浮按钮可以停留在屏幕中间，只有靠近左右边缘时才吸附；桌面端和手机端分别保存位置。
- 拖动和点击使用移动阈值区分，避免移动按钮时误打开菜单。
- 文本输入框获得焦点时，悬浮按钮变为圆形半透明状态；操作悬浮按钮会先取消输入焦点，避免手机输入法意外弹出。
- 当前实现使用普通列表菜单，不包含转轮或径向菜单。

### 最新 UI 自动授权

- 修复最新 UI 首页没有活动 `sessionID` 时，“自动接受权限”开关被错误禁用的问题。
- 有活动会话时继续使用会话目录和会话级授权。
- 首页没有活动会话时，使用当前选中项目目录和目录级授权。
- 会话存在但目录尚未解析时保持禁用，避免把授权错误应用到其他项目。
- 自动批准使用会话级 `always` 响应，同一会话后续遇到相同权限模式时不再重复询问。
- Windows 项目目录在保存授权状态前统一规范化正反斜杠和尾部斜杠，同时继续兼容已经保存在浏览器中的历史授权键。

### 手机端新版输入栏

- 长模型名称只能占用发送按钮之外的剩余空间，超出宽度时单行省略。
- 模型、Agent 和发送控件允许在窄屏内正确收缩，发送按钮始终保持固定尺寸和完整点击区域。
- 开发通道的 Tabs 介绍卡和视频只在 `640px` 及以上渲染，避免手机端遮挡会话列表或产生约 `13 MB` 的额外视频传输；手机帮助按钮避开发送区，桌面端保持官方位置。
- 桌面端继续使用原有最大宽度和排列方式。

## 兼容性和数据

- 修改范围主要位于 `packages/app`，继续使用官方 OpenCode CLI、server API 和数据格式。
- 不修改 `opencode.db` 数据库结构，不执行数据库迁移，也不会复制或替换用户历史记录。
- 不修改或替换官方 OpenCode EXE。
- 同一个 OpenCode 数据目录可以继续由官方桌面端、CLI 和本 Web 前端读取。

建议在首次部署前备份 OpenCode 数据目录。虽然本分支没有数据库迁移，但任何非官方构建都应保留可恢复副本。

## 获取和构建

```bash
git clone https://github.com/Qiguiqiang/opencode.git
cd opencode
git checkout custom-web-1.17.20
bun install

cd packages/app
bun run build
```

正式前端输出位于 `packages/app/dist`。该目录需要连接到兼容的 OpenCode server；本分支不会生成或替换官方桌面 EXE。

本地开发时分别启动后端和前端：

```bash
# 终端 1
cd packages/opencode
bun run --conditions=browser ./src/index.ts serve --port 4096

# 终端 2
cd packages/app
bun dev -- --port 4444
```

打开 `http://localhost:4444`。前端默认连接 `http://localhost:4096`。

## 验证结果

- 与本地修改直接相关的 9 个测试文件共 `158` 项通过，`0` 项失败，包含自动授权、服务会话、项目排序、项目切换、悬浮交互和全局同步子存储测试。
- `packages/app` 正式构建通过。
- `packages/app` 类型检查当前失败。失败文件与官方 `v1.17.20` 标签内容一致，错误集中在官方新增的模型列表、Effect 和时间线类型；旧版在相同工具链下通过，因此没有用本地补丁掩盖这项官方基线回归。
- 在 `390 x 844` 手机视口中实际选择 `DeepSeek V4 Flash` 后，模型按钮与发送按钮重叠为 `0 px`，发送按钮完整位于视口内。
- 同一手机视口中，Tabs 介绍卡未渲染、介绍视频请求数为 `0`，帮助按钮与发送按钮的重叠面积为 `0 px`；项目和会话均可直接点击，切换后输入框未自动获得焦点。

### 冷缓存加载参考

以下数据来自 `390 x 844` 手机视口、新版正式构建和隔离的同源代理。测量时清除 HTTP 缓存但保留站点设置；网络与机器不同会产生波动：

- 两次冷缓存复测中，项目列表约 `0.31-0.52 秒`可见。
- `F:/augment/suraAI-codex-web` 会话摘要约 `1.04-1.17 秒`可用；单次请求实测耗时约 `0.59 秒`，正文 `5,289 B`，传输 `5,589 B`。
- 首屏总传输约 `4.80 MB`：静态资源约 `4.29 MB`，API 约 `0.51 MB`。
- 项目接口正文约 `79,575 B`。代理启动脚本和应用初始化各请求一次项目列表，因此冷启动会看到两次 `/project` 请求。
- 首页会并发读取已展示项目的会话摘要，本次共 `37` 个 `/session` 列表请求；首次加载的消息正文请求数为 `0`。
- 点击具体会话后才请求 `/session/{id}/message?limit=2`，再按需分页读取历史消息；本次目标长会话的后续分页正文约 `1.02 MB`，没有计入首屏传输。
- 手机端即使是全新浏览器状态也不加载 Tabs 介绍视频，首屏仍约 `4.80 MB`。桌面端保留官方介绍卡，首次展示时仍会额外加载该视频。

## 与官方版本对比

- 官方基线：`v1.17.20`
- 自定义分支：`custom-web-1.17.20`
- GitHub 对比：[v1.17.20...custom-web-1.17.20](https://github.com/Qiguiqiang/opencode/compare/v1.17.20...custom-web-1.17.20)

后续同步官方版本时，应先重新运行应用测试、类型检查和浏览器交互验证，再更新自定义分支。若官方基线仍有类型错误，需要分别记录官方错误和本地新增错误，不能把构建成功写成类型检查成功。
